name: Deploy

on:
  push:
    branches:
      - master # Ganti dengan nama branch utama Anda

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Npm Install
        run: npm install

      - name: Run deploy.sh
        run: |
          bash $GITHUB_WORKSPACE/deploy.sh
